// Stands in for the `server-only` package under Vitest, which runs outside the
// React Server Components bundler the real package expects. Wired up in
// vitest.config.ts; never part of the app build.
export {};
